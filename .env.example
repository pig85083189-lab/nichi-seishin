# 在 Vercel Project Settings → Environment Variables 設定。
# 勾選 Production（必要時 Preview / Development 也勾）。
# 改完變數後必須 Redeploy，函式才讀得到新值。
# 不要把真實金鑰提交到 GitHub。

# 建議：Claude（Anthropic）。有設 ANTHROPIC_API_KEY 時，/api/chat、/api/review、週月報一律優先走 Claude Haiku。
ANTHROPIC_API_KEY=sk-ant-replace-me
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# 或者：OpenAI 官方 API（沒有 Anthropic 金鑰時才會用到）
OPENAI_API_KEY=sk-replace-me
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

# 或者改用 OpenRouter（二選一即可）
# OPENROUTER_API_KEY=sk-or-replace-me
# OPENAI_MODEL=openai/gpt-4o-mini
# OPENAI_BASE_URL=https://openrouter.ai/api/v1

# 週月報排程（可選）：接上 Vercel KV 後，Cron 才能在沒開網頁時讀到復盤並存報告。
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# CRON_SECRET=replace-me

# Google 登入改走 Supabase Auth（Providers → Google），不再使用 /api/auth/google。

# Supabase Auth（Google 登入）
# 在 Supabase Project Settings → API 複製 URL 與 anon public key。
# 請先在 SQL Editor 執行 supabase/schema.sql
# Authentication → Providers 開啟 Google，填入 Google Cloud 的 Client ID / Secret。
# Authentication → URL Configuration：
#   Site URL = https://nichi-seishin.vercel.app
#   Redirect URLs 必須包含：
#     https://nichi-seishin.vercel.app/**
#     https://nichi-seishin.vercel.app/auth/callback.html
# Google Cloud Authorized redirect URIs 必須是（不是網站網址）：
#   https://zmjfbdtwxuawebwnybfp.supabase.co/auth/v1/callback
# SUPABASE_URL=https://xxxx.supabase.co
# 前端／anon：SUPABASE_ANON_KEY 或 SUPABASE_PUBLISHABLE_KEY
# 後端寫入訂閱表必須是 Secret / service_role，不是 anon：
# SUPABASE_SERVICE_ROLE_KEY=
# 若你是用 Vercel ↔ Supabase 整合，變數名可能是 SUPABASE_SECRET_KEY，程式兩者都會讀。
# PostgREST schema cache 還沒看到 nichi_internal_users 時，可用 server-side allowlist：
# NICHI_INTERNAL_USER_IDS=uuid,uuid
# NICHI_INTERNAL_EMAILS=you@example.com,teammate@example.com

# 藍新金流（一次付清 MPG）
# MerchantID / HashKey / HashIV 已硬編碼為 HTC109030010100 商店，
# 程式不會再讀取 NEWEBPAY_MERCHANT_ID、NEWEBPAY_HASH_KEY、NEWEBPAY_HASH_IV
# 或任何同義環境變數。
# 閘道環境仍可用 NEWEBPAY_ENV 切換（未設定則正式站）：
#   NEWEBPAY_ENV=prod → https://core.newebpay.com/MPG/mpg_gateway
#   NEWEBPAY_ENV=test → https://ccore.newebpay.com/MPG/mpg_gateway
# NotifyURL 固定為：https://nichi-seishin.vercel.app/api/pay/notify
# ReturnURL 固定為：https://nichi-seishin.vercel.app/api/pay/return
# MPG 金額固定 399，Version 固定 2.0，ItemDesc 固定「日精進」。
# NEWEBPAY_ENV=prod
